/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "bir-music.com",
            "musicdel.ir",
            "next1.ir",
            "musicfile.ir",
            'rozmusic.com',
            'asrmusics.com',
        ]
    }
};

export default nextConfig;
